# ============================================================
# Shop-owner frontend env vars (Vite) — set in Vercel → Environment.
# Vite only exposes vars prefixed with VITE_. Public values only.
# For local dev use .env.development (localhost-only).
# ============================================================

VITE_API_URL=""
VITE_SOCKET_URL=""
